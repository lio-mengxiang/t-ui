import { ImageTypography, AvatarM, CodeBlock, H1, Divide, H3Sub, Code } from '@/_components/typography';
import { IconMuiLogo, Button } from '@t-headless-ui/react';
import { TAnchor } from '@/_components/ui-lib';
import { CodePreview } from '@/_components/code-preview';
import { BasicExample, ChangePageSizeExample, JumperExample, TotalExample, BufferSizeExample, SimpleJumperExample } from '../example';

{/* ### Basic */}

<H3Sub sub={<>The simplest usage.</>}>{'Basic'}</H3Sub>
<BasicExample />

{/* ### Change Page Size */}

<H3Sub sub={<>Define the number of items to display per page.</>}>{'Change Page Size'}</H3Sub>
<ChangePageSizeExample />

{/* ### Jump */}

<H3Sub sub={<>Input the page number to quickly jump to the specified page.</>}>{'Jump'}</H3Sub>
<JumperExample />

{/* ### Total */}

<H3Sub sub={<>Display the total number of items.</>}>{'Total'}</H3Sub>
<TotalExample />

{/* ### Buffer Size */}

<H3Sub
  sub={
    <>
You can use <Code>bufferSize</Code> to set the number of pages between <Code>current</Code> and <Code>...</Code>. A single <Code>...</Code> represents at least <Code>2</Code> omitted pages.
</>

}

> {'Buffer Size'}

</H3Sub>
<BufferSizeExample />

{/* ### Simple Jumper */}

<H3Sub sub={<>In scenarios where space is limited, you can use the simple jumper component below.</>}>{'Simple Jumper'}</H3Sub>
<SimpleJumperExample />

import { ImageTypography, AvatarM, CodeBlock, H1, Divide, H3Sub, Code } from '@/_components/typography';
import { IconMuiLogo, Button } from '@t-headless-ui/react';
import { TAnchor } from '@/_components/ui-lib';
import { CodePreview } from '@/_components/code-preview';
import {
  BasicExample,
  SeparatorsExample,
  ClearAllExample,
  DisableExample,
  PrefixExample,
  MaxTagCountExample,
  CustomTagExample,
} from '../example';

{/* ### Basic */}

<H3Sub
  sub={
    <>
      After pressing Enter, the entered text becomes a tag. Tags that are empty, contain only spaces, or already exist will be filtered out.
    </>
  }
>
  {'Basic'}
</H3Sub>
<BasicExample />

{/* ### Batch Input */}

<H3Sub
  sub={
    <>
      You can use <Code>tokenSeparators</Code> to define separators for batch input. Multiple separators are supported by passing a
      <Code>{'string[]'}</Code>. The following demo is supported to use <Code>{','}</Code> as a separator. Please try copying the text below
      into the input box.
    </>
  }
>
  {'Batch Input'}
</H3Sub>
<SeparatorsExample />

{/* ### Clear All */}

<H3Sub
  sub={
    <>
      Use <Code>allowClear</Code> to enable one-click removal of all tags and the input value. This feature is implemented as a custom
      extension.
    </>
  }
>
  {'Clear All'}
</H3Sub>
<ClearAllExample />

{/* ### Disabled/ReadOnly */}

<H3Sub>{'Disabled/ReadOnly'}</H3Sub>
<DisableExample />

{/* ### Prefix/Suffix */}

<H3Sub
  sub={
    <>
      You can define prefix and suffix elements inside <Code>InputTag.Root</Code> as children.
    </>
  }
>
  {'Prefix/Suffix'}
</H3Sub>
<PrefixExample />

{/* ### Max Tag Count */}

<H3Sub
  sub={
    <>
      Use <Code>maxTagCount</Code> to limit the maximum number of visible tags.
      <Code>maxTagCount.render</Code> allows you to customize the <Code>+x</Code> indicator.
    </>
  }
>
  {'Max Tag Count'}
</H3Sub>
<MaxTagCountExample />

{/* ### Custom Tag */}

<H3Sub
  sub={
    <>
      Customize the tag node by providing <Code>renderTag</Code>.
    </>
  }
>
  {'Custom Tag'}
</H3Sub>
<CustomTagExample />

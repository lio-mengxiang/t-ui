import { ImageTypography, AvatarM, CodeBlock, H1, Divide, H3Sub, Code } from '@/_components/typography';
import { IconMuiLogo, Button } from '@t-headless-ui/react';
import { TAnchor } from '@/_components/ui-lib';
import { CodePreview } from '@/_components/code-preview';
import { BasicExample, NestedExample, ContainerExample, CustomCursorExample, Custom2CursorExample } from './example';
import { TLineAnchorCode, TBallAnchorCode } from '../../../../_t-anchor';

The code below is the advanced component TAnchor, wrapped around the original headless component (`import { Anchor } from '@t-headless-ui/react'`) in the "Basic Usage" section.  
The source code is already shown in "Basic Usage".

If you want to wrap your own higher-level component based on the headless component ({"import { Anchor } from '@t-headless-ui/react'"}), you can refer to the implementation of TAnchor.

{/* ### Basic */}

<H3Sub sub={<>Clicking on an anchor will jump to the specified position.</>}>{'Basic'}</H3Sub>
<BasicExample />

{/* ### Nested Anchor */}

<H3Sub sub={<>Anchors with nested relationship. Suitable for complex scenarios where anchors have parent-child relationships.</>}>
  {'Nested Anchor'}
</H3Sub>
<NestedExample />

{/* ### Specify Container */}

<H3Sub sub={<>Specify the container to scroll when clicking on an anchor. Useful when anchors are inside a scrollable container.</>}>
  {'Specify Container'}
</H3Sub>
<ContainerExample />

{/* ### Custom1 */}

<H3Sub
  sub={
    <>
      Custom cursor style:
      <TAnchor
        offset={-90}
        className="inline-flex underline text-primary-color cursor-pointer"
        items={[
          {
            text: 'Click to jump to source code',
            id: 'Custom1',
          },
        ]}
      />
      。
    </>
  }
>
  {'Custom1'}
</H3Sub>
<CustomCursorExample />

{/* ### Custom2 */}

<H3Sub
  sub={
    <>
      Custom cursor style:
      <TAnchor
        offset={-90}
        className="inline-flex underline text-primary-color cursor-pointer"
        items={[
          {
            text: 'Click to jump to source code',
            id: 'Custom2',
          },
        ]}
      />
      。
    </>
  }
>
  {'Custom2'}
</H3Sub>
<Custom2CursorExample />

{/* ### Custom1 Code */}

<H3Sub>{'Custom1 Code'}</H3Sub>
<TLineAnchorCode />

{/* ### Custom2 Code */}

<H3Sub>{'Custom2 Code'}</H3Sub>
<TBallAnchorCode />
